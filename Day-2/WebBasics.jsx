import React from 'react';

function WebBasics() {
  return (
    <div className="web-basics">
  <h2>The Building Blocks of the Web</h2>
  <p>HTML gives structure, CSS brings style, and JavaScript adds life — all working together in this single component.</p>
  <p>This is a live demonstration of HTML + CSS + JS unified through JSX.</p>
    </div>
  );
}

export default WebBasics;